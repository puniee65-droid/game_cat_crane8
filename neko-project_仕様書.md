# ねこキャッチャー ― システム構成・機能仕様書

3Dクレーンゲームで捕まえた猫が「マイルーム」で暮らす、Vue.js + Three.js + Node.jsのWebアプリケーション。

---

## 目次

1. [システム構成](#1-システム構成)
2. [起動方法](#2-起動方法)
3. [データモデル（サーバ）](#3-データモデルサーバ)
4. [API仕様](#4-api仕様)
5. [クレーンゲームの機能仕様](#5-クレーンゲームの機能仕様)
6. [マイルームの機能仕様](#6-マイルームの機能仕様)
7. [3Dねこメーカーの機能仕様](#7-3dねこメーカーの機能仕様)
8. [共通データ形式](#8-共通データ形式)
9. [ゲーム⇔マイルーム間の連携の仕組み](#9-ゲームマイルーム間の連携の仕組み)
10. [使い方（プレイヤー向け）](#10-使い方プレイヤー向け)
11. [今後の拡張予定](#11-今後の拡張予定)
12. [開発メモ・既知の注意点](#12-開発メモ既知の注意点)

---

## 1. システム構成

```
neko-project/
├─ server.js               … APIサーバ + 静的配信（Node.js標準ライブラリのみ、依存パッケージ0個）
├─ package.json
├─ data/db.json             … 実行時に自動生成されるJSONストレージ（ユーザーデータ）
└─ public/
   ├─ game.html             … クレーンゲーム画面（Vue.js製ヘッダー/フッター）
   ├─ room.html             … マイルーム画面（Vue.js製ヘッダー/フッター）
   ├─ cat_model_maker.html  … 猫の写真から3Dモデルを作る単体ツール
   ├─ assets/
   │  └─ white_cat_video.mp4   … 白ねこクリック時に再生する動画
   └─ js/
      ├─ cat-shared.js      … 猫の3Dモデル定義・ビルダー（ゲーム/ルーム/メーカー共通）
      ├─ game-engine.js     … クレーンゲームの3Dロジック（Three.js。UIから分離）
      └─ room-engine.js     … マイルームの3Dロジック（Three.js。UIから分離）
```

### 技術スタック

| 層 | 技術 |
|---|---|
| 3D描画 | Three.js r128（cdnjs読み込み） |
| UI（ヘッダー/フッター/メニュー/ポップアップ） | Vue.js 3（cdnjs読み込み、CDN版のためビルド不要） |
| サーバ | Node.js標準の`http`モジュールのみ（Express等の依存なし） |
| データ保存 | JSONファイル（`data/db.json`）。将来はCloudflare D1/Durable Objects等への差し替えを想定した設計 |

### 設計方針

- **UI(Vue) と 3Dエンジン(Three.js) の分離**：`game-engine.js`/`room-engine.js`は`createGameEngine(HOOKS)`/`createRoomEngine(HOOKS)`という関数として実装され、DOM操作を直接行わず、`HOOKS.onHud`や`HOOKS.showMsg`などのコールバックでVue側に状態を通知する。逆にVue側からはエンジンが返す関数（`purchase()`, `feedCat()`等）を呼び出す。
- **サーバAPIが唯一の正**：コイン・特点・エサ・家具の所持状況・獲得猫は、すべてサーバ（`db.json`）が真の情報源。クライアントは表示用に一時的にキャッシュするが、定期的にサーバへ問い合わせて最新化する。
- **Cloudflare移行を見据えた設計**：`server.js`のAPIハンドラ`handleApi(method, path, body, store)`はNode固有のreq/resに依存しない純関数として実装されており、Cloudflare Workersの`fetch`ハンドラへそのまま移植できる。ストレージも`Store`インターフェース（`getUser`/`putUser`）越しにアクセスしているため、実装をKVやD1に差し替えるだけで移行できる。

---

## 2. 起動方法

```bash
cd neko-project
node server.js
# または: npm start
```

起動すると以下が表示される：

```
ねこキャッチャー サーバ起動:
  PC:     http://localhost:3000/game.html
  スマホ: http://192.168.x.y:3000/game.html   (同じWi-Fi内から)
```

- サーバは`0.0.0.0`で待ち受けるため、**同じWi-Fi内のスマホ・タブレットからもアクセス可能**
- `npm install`は不要（依存パッケージ0個）
- 初回起動時に`data/db.json`が自動生成される

---

## 3. データモデル（サーバ）

`data/db.json`は以下の構造を持つ（1ユーザー分）：

```json
{
  "userId": "u_xxxxxxxxxxxx",
  "createdAt": 1783406746355,
  "createdAtJst": "2026-07-07 15:45:46 JST",
  "coins": 5,
  "points": 0,
  "score": 0,
  "food": 0,
  "cats": [
    { "id": "c1", "def": { "...": "猫の色・体型・レインボー等の定義" },
      "caughtAt": 1783406746500, "caughtAtJst": "2026-07-07 15:45:46 JST" }
  ],
  "catCounts": { "白ねこ": 2, "黒ねこ": 1 },
  "furniture": { "tower": false, "sofa": false, "cushion": false, "house": false },
  "sessions": [
    { "id": "s_xxxxx", "startedAt": 1783406746479, "startedAtJst": "...",
      "endedAt": 1783406747508, "endedAtJst": "..." }
  ]
}
```

- 時刻は**エポックms**（計算・ソート用）と**日本時間(JST)の文字列**（`Intl.DateTimeFormat`の`Asia/Tokyo`指定で生成）を両方保持
- `userId`はブラウザの`localStorage['nekoUserId']`に保存され、以後のアクセスに使われる（現状は認証なしの匿名ID方式。詳細は[11章](#11-今後の拡張予定)）

---

## 4. API仕様

すべて`Content-Type: application/json`。ベースURL: `http://<host>:3000`

| メソッド | パス | 用途 |
|---|---|---|
| POST | `/api/user` | ユーザー確保（`{userId}`が未登録/nullなら新規発行） |
| GET | `/api/user/:id` | ユーザー情報取得 |
| PATCH | `/api/user/:id` | コイン・特点・ゲット数の同期（`{coins,points,score}`） |
| POST | `/api/session/start` | プレイ開始時刻を記録 |
| POST | `/api/session/end` | プレイ終了時刻を記録（`sendBeacon`でタブを閉じる際も確実に送信） |
| POST | `/api/catch` | 獲得猫を記録（`entry.id`で冪等。同じ猫は二重計上されない） |
| GET | `/api/cats/:userId` | 獲得猫一覧の取得（マイルームの復元・ポーリングに使用） |
| DELETE | `/api/cats/:userId` | 猫をリセット（獲得履歴のクリア） |
| POST | `/api/furniture` | 家具購入（`{userId,item}`。価格はサーバの`FURNITURE_PRICES`が正。コイン不足なら400エラー） |
| DELETE | `/api/furniture/:userId` | 家具をリセット（コインは返却されない） |
| POST | `/api/coins/buy` | コイン購入（現状は仮実装で常に+5。将来のStripe等決済導入ポイント） |
| POST | `/api/food/buy` | エサ（魚フレーク）購入。`{userId,qty}`、1個1コイン |
| POST | `/api/food/use` | 猫にエサをあげる（所持エサを1消費） |
| POST | `/api/points/convert` | 特点をコインに変換（10点→1コイン、端数は特点として残る） |

### 価格表（`server.js`内 `FURNITURE_PRICES`）

| 家具 | 価格 |
|---|---|
| 猫タワー | 5コイン |
| ソファー | 5コイン |
| クッション | 3コイン |
| 猫小屋 | 5コイン |

---

## 5. クレーンゲームの機能仕様（`game.html` + `game-engine.js`）

### 画面構成
- **ヘッダー**：タイトル、ステージ数・コイン・ゲット数・とくてんを表示
- **メイン**：3Dビュー（Three.js）
- **フッター**：方向パッド（アーム移動）・落下ボタン・「🏠 マイルーム」「💰 コイン投入」ボタン

### ステージ制
- **ステージ1**：白ねこ1匹のみ
- **ステージ2**：茶ねこ・黒ねこ2匹
- **ステージ3**：ピンクねこ・レインボーねこ2匹
- **ステージ4以降**：5色（白・茶・黒・ピンク・レインボー）を2匹ずつ巡回しながら無限に継続
- そのステージの必要数を全部捕まえると「🎉 ステージN完了！」と表示され、次のステージが自動でスポーンする

### 猫の特点
| 色 | 特点 |
|---|---|
| 白ねこ | 5点 |
| 茶ねこ | 10点 |
| 黒ねこ | 20点 |
| ピンクねこ | 40点 |
| レインボーねこ | 80点（体の左右方向に流れる7色の縞々） |

### クレーン操作
- 矢印キー / 画面の方向パッドでアーム移動
- スペースキー / 赤ボタンで落下
- **鷲掴み判定**：爪の指を1本ずつ独立に閉じ、猫の体（楕円体近似）に触れた位置で止める干渉チェック方式。中心からのズレ・運・触れた指の本数で握力が決まり、弱いと運搬中に滑り落ちる
- **香箱座り中の猫**：掴み判定半径が拡大し、握力もほぼ確実に保証される「狙い目」

### 猫の行動
- 徘徊（歩く⇔立ち止まる）、香箱座り（目を閉じる）、舌なめずり、猫同士の物理的な重なり防止（体+頭の2円判定）

### 隠しキー（デバッグ用）
| キー | 動作 |
|---|---|
| `A` | 猫の動きを一時停止/再開（クレーン操作は継続可能） |
| `B` | フィールドの猫の数を入力して作り直し（1〜20） |

### コイン・特点の同期
- ゲームは自身の操作でコイン/特点/ゲット数が変わるたびサーバへ`PATCH`（800msデバウンス）
- 同時に2.5秒おきにサーバをポーリングし、**マイルーム側での変化（エサ購入・家具購入・特点コイン化等）を取り込む**
- ローカルの変更がサーバへ送信中（デバウンス待ち）の間はポーリング更新をスキップし、上書き競合を防止

---

## 6. マイルームの機能仕様（`room.html` + `room-engine.js`）

### 部屋の構成
- 内寸10×10、薄茶色のフローリング、白い壁
- **右壁(+x)が掃き出し窓**（庭が見える。窓のない他3面は無地の壁）
- 初期状態では家具は一切設置されていない（☰メニューの「お買い物」で購入すると出現）
- カメラ側の壁は自動的に非表示になる（ドラッグ回転・ホイール/ピンチズーム対応）
- 庭には芝生・木・うねった小道があり、猫は窓を経由して出入りする

### ヘッダー
- 1行目：「🎮 ゲームへ戻る」（ゲームから開かれた場合のみ）／タイトル／「☰ メニュー」ボタン
- 2行目：ねこ数・とくてん・コイン・エサの表示、「❓ 使い方」ボタン
- スマホの狭い画面でも文字が縦書きにならないよう、統計行が独立した2行目としてラップする設計

### ☰メニュー（ハンバーガーメニュー、何か押すと自動的に閉じる）
| 項目 | 内容 |
|---|---|
| 🛒 家具を買う | サブメニュー展開。猫タワー/ソファー/クッション/猫小屋をコインで購入 |
| 🐟 エサを買う | 魚フレークを1コインで購入 |
| 💰 コイン購入 | +5コイン（将来の決済導入までの仮実装） |
| ✨ とくてんをコインにする | 10点→1コインで変換 |
| 🪑 家具をリセット | 設置済み家具を全撤去（コインは返却されない） |
| 🐱 猫をリセット | 部屋の猫をすべてクリア |
| 🏠 ホームページに戻る | トップページへ遷移 |

### 猫の行動フロー
```
転送出現 → 窓の外を見る → 奥の壁を背に着席（窓に近い側から順、猫タワーの設置場所は自動で回避）
```
- 着席の間隔は「猫一匹分」
- 着席後は立ち姿勢⇔香箱座り（目を閉じる）を自動で繰り返す
- **睡眠**：1時間クリック等のインタラクションがないと自動的に眠る（クリックすると即座に起きる）
- 隠しキー**「C」**：全猫を強制的に眠らせる（デバッグ用、1時間待たずに確認できる）

### 猫クリック時のポップアップ（白ねこのみ）
猫をクリック/タップすると動画（ループ再生・画面いっぱいに大きく表示）のポップアップが開き、下に4つのアクションボタンが並ぶ：

| ボタン | 動作 |
|---|---|
| 🐟 エサをやる | 所持エサを1消費。エサが0なら「エサを買ってください」と表示 |
| 🧶 部屋で遊ぶ | 所持家具からランダムに選び、タワーなら登る、ソファー/クッションなら跳び乗る、猫小屋なら中に入る演出後、元の定位置へ帰る |
| 🌿 冒険する | 窓を経由して庭へ出て、ランダムな方向・距離まで歩き、端に着いたら窓を経由して部屋に戻り、定位置へ着席する |
| 📜 今日のことば | 日本時間の日付でシードされた10種のフレーズの中から1つを表示（同じ日は同じ言葉） |

クリック（どの猫でも）は「インタラクション」として扱われ、眠っている猫はこれで起きる。

### 物理的な干渉チェック
猫同士は「体＋頭の2円」判定で重なりを防止（クレーンゲームと同じ手法）。庭を歩く際は室内の床(y=0)と庭の地面(y=-0.32)の高さの違いを、窓の幅の区間でなだらかに補間して足が浮かない/めり込まないようにしている。

### コイン・特点・エサの同期
- 起動時にサーバから所持家具・獲得猫・コイン/特点/エサを復元
- 2.5秒おきにサーバをポーリングし、ゲーム側での変化（コイン投入・特点獲得等）を反映

---

## 7. 3Dねこメーカーの機能仕様（`cat_model_maker.html`）

猫の写真から、ゲーム/ルームで使える3Dモデル定義ファイル（`.neko.json`）を作る単体ツール。

1. **写真をドロップ**（透過PNG推奨。単色背景なら四隅から背景色を推定して除去）
2. **色の自動抽出**：猫領域のピクセルをk-means（k=5）でクラスタリングし、毛色/模様/縞に自動割り当て
3. **スポイト機能**：画像をクリックして特定部位（目・鼻など）の色を直接採取
4. **体型スライダー**：頭の大きさ・胴の長さ・脚の長さ・耳・目・ふわふわ度・模様の量・全体サイズをリアルタイムプレビューしながら調整
5. **書き出し**：`名前.neko.json`としてダウンロード

`buildCatModel()`は`cat-shared.js`としてゲーム/ルームと共通化されており、メーカーでのプレビューと実際の見た目が一致する。

---

## 8. 共通データ形式 `neko-crane-cat`

```json
{
  "format": "neko-crane-cat",
  "version": 1,
  "name": "たま",
  "points": 10,
  "rainbow": false,
  "colors": {
    "fur": "#f3f1ea", "patch": "#b59c82", "stripe": "#8a715c",
    "earIn": "#c9a290", "iris": "#6d5c3c", "nose": "#b97f84"
  },
  "props": {
    "headSize": 1, "legLen": 1, "earSize": 1, "eyeSize": 1,
    "fluff": 1, "patchAmount": 1, "bodyLen": 1, "scale": 1
  }
}
```

- `points`省略時は10点。`"rainbow": true`を足すと任意の猫を縞々レインボー化できる
- クレーンゲームは現在、猫の読み込み機能（インポートボタン）を廃止しステージ制の自動スポーンのみを使用しているが、このJSON形式自体は`cat_model_maker.html`と`cat-shared.js`の間で共通利用されている

---

## 9. ゲーム⇔マイルーム間の連携の仕組み

### 猫の転送
1. ゲームで猫を落とし口に入れると、テラスで1秒待機
2. テレポート演出（縮小＋回転）とともに`/api/catch`へPOSTし、サーバに永続化
3. 同時に、🏠ボタンで開いたマイルームのウィンドウへ`postMessage({type:'neko-transfer', entry})`で即時通知（開いていれば即座に反映される）
4. マイルームは起動時と2.5秒おきに`/api/cats/:userId`をGETし、サーバ側の獲得履歴と同期（リロードしても復元される）

### 画面の自動切り替え
- ゲームの「🏠 マイルーム」ボタンは`window.open`でマイルームを開き、以後`focus()`で前面化できる（ブラウザの制約上、自分が開いたウィンドウへのフォーカスのみ許可されるため）
- マイルームの「🎮 ゲームへ戻る」は、`window.open('', 'nekoGame')`による**名前付きウィンドウの再ターゲット**方式（`opener.focus()`は多くのブラウザで無視されるため、この方式が確実）

### コイン・特点の双方向同期
- 両画面とも自分の変更をサーバへ書き込み、2.5秒おきに相手側の変更をポーリングで取り込む
- 送信中（デバウンス待ち）はポーリング更新をスキップし、古い値で上書きしてしまう競合を回避

---

## 10. 使い方（プレイヤー向け）

### 基本の流れ
1. `node server.js`でサーバを起動し、`http://localhost:3000/game.html`を開く
2. ステージ1（白ねこ）からクレーンゲームに挑戦。矢印キー/D-padでアームを動かし、スペース/赤ボタンで落とす
3. 捕まえた猫は自動でマイルームへ転送される（🏠ボタンでマイルームを開いておくと自動的に画面が切り替わる）
4. マイルームでは☰メニューから家具を購入したり、エサ・コインを買ったり、とくてんをコインに変換したりできる
5. 白ねこをクリックすると動画ポップアップが開き、「エサをやる」「部屋で遊ぶ」「冒険する」「今日のことば」を楽しめる

### スマホでのアクセス
サーバ起動時に表示される`http://192.168.x.y:3000/game.html`を、同じWi-Fiに繋いだスマホのブラウザで開く。

### 隠しコマンド一覧
| 画面 | キー | 動作 |
|---|---|---|
| ゲーム | `A` | 猫の動きを一時停止/再開 |
| ゲーム | `B` | 猫の数を入力して作り直し |
| マイルーム | `C` | 全猫を強制的に眠らせる |

---

## 11. 今後の拡張予定

- **決済**：`FURNITURE_PRICES`・`/api/coins/buy`・`/api/food/buy`には、将来Stripe等の決済を組み込む前提のTODOコメントを付与済み
- **認証**：現状は`localStorage`に置いた乱数IDによる匿名ユーザー方式。本人確認が必要になった場合、Firebaseを使わずに**Cloudflare Access**（Google/GitHub/メールOTPログインをCloudflare側が提供し、検証済みIDがWorkerに渡る）へ直接移行する方針を想定。現在の`userId`ベースの`Store`インターフェースはそのまま活かせる
- **ストレージ**：`JsonFileStore`から**Cloudflare D1**（関係データ向き）または**Durable Objects**（ユーザーごとの状態管理としてCloudflare公式が推奨するパターン）への移行を想定。`handleApi()`はNode非依存の純関数のため、Workersの`fetch`ハンドラへそのまま移植可能
- **動画配信**：`assets/`配下の動画は、Cloudflare R2への配置を想定。HLS化は不要（数秒の単発再生クリップのため）で、`faststart`フラグの付与とH.264/AACコーデックの維持を推奨。R2からのRangeリクエスト対応は、Workerを経由しない公開バケット配信であれば概ね問題ない

---

## 12. 開発メモ・既知の注意点

- **ブラウザキャッシュ**：ロジックを修正しても、ブラウザがJSファイルをキャッシュしていると反映されないことがある。確認時はスーパーリロード（Ctrl+Shift+R / Cmd+Shift+R）を推奨
- **座標変換の検証**：Three.jsの回転（`rotation.y`）が実際にどの世界座標方向を向くかは、暗算では間違えやすい。疑わしい場合はNode.jsに`three`パッケージを一時インストールし、`Object3D`を生成して`applyQuaternion`で実際に検証するのが確実（本プロジェクトの座席の向きバグ修正で実施済み）
- **ポーリングによる上書き競合**：クライアントの値をサーバへデバウンス送信する仕組みと、サーバの値をポーリングで取り込む仕組みを両方持つ場合、「送信中に古い値で上書きされる」競合が起きやすい。送信中フラグ（`pendingPersist`等）でポーリング側を待たせるのが簡便な対策
- **モバイルの表示崩れ**：`100vh`はモバイルブラウザのアドレスバー分を考慮しないため、`100dvh`・JS計測の`--vh`・`visualViewport`・`-webkit-fill-available`を組み合わせて対応している
- **file://での動作制限**：ブラウザによってはfile://をファイルごとに別オリジンとして扱うため、`localStorage`の共有に依存する設計は避け、`postMessage`とサーバAPIを主経路にしている
